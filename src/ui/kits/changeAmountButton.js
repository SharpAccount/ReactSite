import {useState} from "react";

const [amount, increaseAmount, decreaseAmount] = useState(0);

//place for increase decrease buttons
// methods will change amount value
// amount will render as react component and
// import into addToCard file